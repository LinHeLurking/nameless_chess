# CMakeList.txt : CMake project for nameless_chess_cpp, include source and define
# project specific logic here.
#
cmake_minimum_required (VERSION 3.8)



set (OUT_DIR "../../..")

file(MAKE_DIRECTORY ${OUT_DIR}/lib)


set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY_DEBUG ${OUT_DIR}/lib)
set(CMAKE_LIBRARY_OUTPUT_DIRECTORY_DEBUG ${OUT_DIR}/lib)
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY_DEBUG ${OUT_DIR}/lib)

set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY_RELEASE ${OUT_DIR}/lib)
set(CMAKE_LIBRARY_OUTPUT_DIRECTORY_RELEASE ${OUT_DIR}/lib)
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY_RELEASE ${OUT_DIR}/lib)



find_package(JNI)

if (JNI_FOUND)
    message (STATUS "JNI_INCLUDE_DIRS=${JNI_INCLUDE_DIRS}")
    message (STATUS "JNI_LIBRARIES=${JNI_LIBRARIES}")
endif()

include_directories (${JNI_INCLUDE_DIRS})

# Add source to this project's executable.
add_library (nameless_chess_cpp SHARED "test_main.cpp" "matrix.h" "Chess_Board.h" "Chess_Board.cpp" "game.h" "game.cpp" "message.h" "dep.h" "api.cpp" "online_ruin_of_future_nameless_chess_CppSync.h")

# TODO: Add tests and install targets if needed.
